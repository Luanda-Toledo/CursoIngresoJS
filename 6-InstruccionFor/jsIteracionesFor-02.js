/*
Al presionar el botón mostrar 10 repeticiones con números , desde el 10 al 1.
*/

function mostrar()
{

	// Declaramos e iniciamos a i con el valor 1
	// La condicion -> i debe ser mayor que cero
	// El contador decrementa uno en cada iteracion
	for (let i = 10; i > 0; i--){

		// Mostramos por consola los numeros
		console.log(i);
	}
}