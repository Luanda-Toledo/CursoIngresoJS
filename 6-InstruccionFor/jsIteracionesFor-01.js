/* 
	Al presionar el botón mostrar 10 repeticiones con números ASCENDENTE, desde el 1 al 10.
*/
function mostrar()
{
	// Declaramos e iniciamos a i con el valor 1
	// La condicion -> i debe ser menor que once
	// El contador aumenta uno en cada iteracion
	for (let i = 1; i < 11; i++){

		// Mostramos por consola los numeros
		console.log(i);
	}
}